spring:
  application:
    name: product-service
  cloud:
    config:
      uri: http://config
      fail-fast: true
      password: ${CONFIG_SERVICE_PASSWORD}
      username: user

server:
  port: 80

constants:
  page: 0
  size: 10