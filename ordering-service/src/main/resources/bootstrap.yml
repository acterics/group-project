spring:
  application:
    name: ordering-service
  cloud:
    config:
      uri: http://config
      fail-fast: true
      password: ${CONFIG_SERVICE_PASSWORD}
      username: user

server:
  port: 80